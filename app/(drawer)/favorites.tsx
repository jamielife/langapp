import { View, Text } from 'native-base';

const Favorites = () => {
    return (
        <View p={3} flex={1} style={{ justifyContent: "center", alignContent: "center" }}>
            <Text style={{ color: "gray" }}>Boku no Favorites desuuuu!</Text>        
        </View>
      );    
}

export default Favorites;